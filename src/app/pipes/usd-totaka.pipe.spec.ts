import { UsdTotakaPipe } from './usd-totaka.pipe';

describe('UsdTotakaPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdTotakaPipe();
    expect(pipe).toBeTruthy();
  });
});
