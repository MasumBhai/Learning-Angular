import { RedifyDirective } from './redify.directive';

describe('RedifyDirective', () => {
  it('should create an instance', () => {
    const directive = new RedifyDirective();
    expect(directive).toBeTruthy();
  });
});
